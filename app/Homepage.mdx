---
title: Markdown Component Loader
---
<center>
<h1>{{<img src={require('./images/logo.svg')} alt="Markdown Component Loader" />}}</h1>

A Webpack loader that turns Markdown into dynamic, stateless React components!

<a class="bubble-link github-link" href="https://github.com/ticky/markdown-component-loader">Check it out on GitHub</a>
</center>

### welcome.mdx
```markdown
{{ <span className="emphasis">Hey there **{'{'+'{ props.who }'+'}'}** ✌🏼</span> }}
```

### app.jsx
```jsx
{{ <span className="emphasis">import Welcome from './welcome.mdx';</span> }}
 
ReactDOM.render(
{{ <span className="emphasis">{'  <Welcome who="Monkey Magic" />,'}</span>}}
{{`  document.getElementById('app')`}}
);
```

### Rendered:
```html
<div id="app">
{{ <span className="emphasis">{'  <p>Hey there <strong>Monkey Magic</strong> ✌️</p>'}</span>}}
</div>
```

## What else can it do?

Embed SVG directly into your mdx file, just as you would a jsx file:

```markdown
Here’s an inline SVG: {{<span className="emphasis">{'{'+'{'} {'<svg style={{ display: \'inline\', height: \'1em\'}} viewBox="0 0 304 290"><path fill="none" stroke="currentColor" strokeWidth="16" d="M2,111 h300 l-242.7,176.3 92.7,-285.3 92.7,285.3 z" /></svg>'} {'}'+'}'}</span>}}. You can see that nested curly braces are also fully supported.
```

Import functions, such as shared snippets or fancy library functions, using YAML front-matter:

```markdown
---
{{<span className="emphasis">{'imports:'}</span>}}
{{<span className="emphasis">{'  window: global/window'}</span>}}
---

Hello <code>{{<span className="emphasis">{'{{ window.navigator.userAgent }}'}</span>}}</code>
```

Import whole React components:

```markdown
---
{{<span className="emphasis">{'imports:'}</span>}}
{{<span className="emphasis">{'  Button: ui/button'}</span>}}
---

Please {{<span className="emphasis">{'{\{ <Button path="/login">Login</Button> }\}'}</span>}}
```

Destructured imports, with some added single quotes:

```markdown
---
{{<span className="emphasis">{'imports:'}</span>}}
{{<span className="emphasis">{'  \'{ Button, Panel }\': ui'}</span>}}
---

Hello <code>{{<span className="emphasis">{'{\{ <Button /> }\}'}</span>}}</code>
```

## Give it a go!

Want to see exactly what Markdown Component Loader will generate when it translates the mdx into a stateless React component?

<div class="repl-image">
  <div>
    <a class="bubble-link repl-link" href="repl.html">Launch editor</a>
  </div>
</div>

## Get started

Check out the [GitHub Readme](https://github.com/ticky/markdown-component-loader) for how to get started, including details on how to add styles and event handlers to the DOM nodes generated by the markdown, and other options for customizing the loader.

<center>
<a class="bubble-link github-link" href="https://github.com/ticky/markdown-component-loader">Check it out on GitHub</a>

---

Made with 💜 by [@ticky](https://twitter.com/ticky)
</center>
